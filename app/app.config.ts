export default defineAppConfig({
  ui: {
    prose: {
      a: {
        base: 'text-primary underline underline-offset-4 dark:text-cyan-400 dark:hover:text-cyan-500 transition-colors'
      }
    }
  }
})
